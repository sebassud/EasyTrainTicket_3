<nav id="navbar-black" class="navbar navbar-default" role="navigation">

    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <span class="visible-xs"><a class="navbar-brand" href="/">Easy Train Tickets</a></span>
    </div>

    <div class="navbar-collapse collapse sidebar-navbar-collapse">

        <ul class="nav navbar-nav">

            <li class="btn">
                <a class="dropdown-toggle" href=@Url.Action("Index","Home")>Home<i class="glyphicon glyphicon-home"></i></a>
            </li>
            @if (!(Request.IsAuthenticated && User.IsInRole("Admin")))
            {
                <li class="btn">
                    <a href=@Url.Action("Index", "Search")>Wyszukaj połączenie<i class="glyphicon glyphicon-search"></i></a>
                </li>
            }
            @if (Request.IsAuthenticated)
            {
                if (User.IsInRole("Admin"))
                {
                    <li class="btn">
                        <a href=@Url.Action("Index","AddConnections")>Dodaj połączenia<i class="glyphicon glyphicon-book"></i></a>
                    </li>
                }
                else
                {
                    <li class="btn">
                        <a href=@Url.Action("Index","MyTicket")>Moje bilety<i class="glyphicon glyphicon-user"></i></a>
                    </li>                    
                }
            }
            <li class="btn">
                <a href=@Url.Action("Contact","Home")>Kontakt<i class="glyphicon glyphicon-earphone"></i></a>
            </li>
        </ul>
        <img  src="~/Content/Images/trainLogo.png" width="250" height="60" class="train"/>
    </div><!--/.nav-collapse -->
</nav><!--/.navbar -->
@Html.Partial("_LoginPartial")
